<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kobiltekmobil.kobalt">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.activity.MainActivity" />
        <activity android:name=".main.activity.DisplayHost" />
        <activity android:name=".main.activity.HostActivity" />
        <activity android:name=".main.activity.ModulesActivity" />
        <activity android:name=".salon.activity.SalonActivity" />
        <activity android:name=".salon.activity.ReservationListActivity" />
        <activity
            android:name=".main.activity.Splash"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".salon.activity.ReservationActivity"
            android:label="@string/title_activity_deneme12"
            android:theme="@style/AppTheme" />
        <activity android:name=".bordro.activity.BordroActivity"></activity>
    </application>

</manifest>